#!/usr/bin/env python3

""" 
for some reason, when I try to import the main function from the main.py file, it gives me an error that the cipherman package is not found, even though it is in the same directory as the setup.py file. So I had to add the project root directory to the Python path before importing the main function.
uncomment the following lines if you want to run the main function directly from this file, otherwise, you can run the main function from the command line using the cipherman command after installing the package.
"""
# import sys
# import os

# # Add the project root directory to the Python path
# sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from cipherman.main import main
main()

